package card;


/**
 * An interface that handles all the possible intrigue cards
 * so only really for the keepers and clocks cards
 * @author scott
 *
 */
public interface IntrigueCard extends Holdable{
	
}
